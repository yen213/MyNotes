<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#1565C0</item>
        <item name="colorPrimaryDark">#1565C0</item>
        <item name="colorAccent">@color/black</item>
        <item name="colorControlNormal">@color/black</item>
    </style>

    <!-- Alternative app theme -->
    <style name="AppTheme.Black" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/darK_grey</item>
        <item name="colorAccent">@color/black</item>
        <item name="colorControlNormal">@color/black</item>
    </style>

    <!-- Collapsing Toolbar Expanded Style -->
    <style name="CollapsingStyleExpanded" parent="@android:style/TextAppearance">
        <item name="android:textSize">40sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Collapsing Toolbar Collapsed Style -->
    <style name="CollapsingStyleCollapsed" parent="@android:style/TextAppearance">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Toolbar Options Menu Style -->
    <style name="AppToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="tint">@color/white</item>
    </style>

</resources>
